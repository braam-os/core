# Size budgets, in bytes. One "name = limit" per line; the name is the file's.
# Raising a number is a deliberate act — see Release_Notes.md.
#
# No per-program entry: a program is measured but not bounded, and what one
# costs is §4.4's duplication, which the archive below shows in one number.
kernel.wasm = 262144

# The boot archive: every binary, plus what ships beside them. Awaited before
# the first prompt, so it is boot latency as well as bytes.
bundle.bin = 1048576
